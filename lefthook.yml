commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-commit:
  commands:
    format:
      glob: "*.{js,ts,tsx,json,css,md,svelte,html}"
      run: yes | biome check --write --colors=off .
      stage_fixed: true
